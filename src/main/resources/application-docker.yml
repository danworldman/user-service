spring:
  datasource:
    url: jdbc:postgresql://db:5432/user_db
    username: ${SPRING_DATASOURCE_USERNAME:danworldman}
    password: ${SPRING_DATASOURCE_PASSWORD:user}
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: redis
      port: 6379