spring:
  application:
    name: user-service
  liquibase:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  profiles:
    active: local
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false